<UserControl x:Class="Plantify.Views.AddUserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Plantify.Views"
             xmlns:vm="clr-namespace:Plantify.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:AddUserViewModel}"
             MinWidth="450">
    <UserControl.Resources>
        <Style x:Key="FormTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource BrushForestGreen20Transparent}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BrushDarkGreen}"/>
            <Setter Property="Foreground" Value="{StaticResource BrushDarkGreen}"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontFamily" Value="{StaticResource FontInter}"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="MinHeight" Value="38"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Padding="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FormComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource BrushForestGreen20Transparent}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BrushDarkGreen}"/>
            <Setter Property="Foreground" Value="{StaticResource BrushDarkGreen}"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontFamily" Value="{StaticResource FontInter}"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="MinHeight" Value="38"/>
        </Style>

        <Style x:Key="FormLabelStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{StaticResource FontInter}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Foreground" Value="{StaticResource BrushDarkGreen}"/>
            <Setter Property="Margin" Value="0,5,0,5"/>
        </Style>
        
        <Style x:Key="ErrorTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource BrushTerracotta}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,2,0,5"/>
        </Style>

        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="15">
                            <TextBlock x:Name="CloseButtonText" Text="✕" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="20" FontWeight="Bold"
                                       Foreground="{DynamicResource BrushGray}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CloseButtonText" Property="Foreground" Value="IndianRed"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="{StaticResource BrushMilkWhite}" CornerRadius="12" Padding="30" Effect="{StaticResource Dp16Shadow}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="Добавить нового пользователя" Style="{StaticResource H1}" TextWrapping="Wrap"/>
                <Button Command="{Binding CancelCommand}" Style="{StaticResource CloseButtonStyle}" 
                        HorizontalAlignment="Right" VerticalAlignment="Top" Width="35" Height="35" Margin="0,-25,-25,0"/>
            </Grid>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,-20,0" Padding="0,0,20,0" Style="{StaticResource ScrollBarStyle}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Логин" Style="{StaticResource FormLabelStyle}"/>
                        <TextBlock Text="*" Foreground="{StaticResource BrushTerracotta}" Style="{StaticResource FormLabelStyle}" Margin="2,5,0,5"/>
                    </StackPanel>
                    <TextBox x:Name="LoginTextBox" Text="{Binding Login, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Style="{StaticResource FormTextBoxStyle}"/>
                    <TextBlock Text="{Binding (Validation.Errors)[0].ErrorContent, ElementName=LoginTextBox}" Style="{StaticResource ErrorTextStyle}" />

                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Email" Style="{StaticResource FormLabelStyle}"/>
                        <TextBlock Text="*" Foreground="{StaticResource BrushTerracotta}" Style="{StaticResource FormLabelStyle}" Margin="2,5,0,5"/>
                    </StackPanel>
                    <TextBox x:Name="EmailTextBox" Text="{Binding Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Style="{StaticResource FormTextBoxStyle}"/>
                    <TextBlock Text="{Binding (Validation.Errors)[0].ErrorContent, ElementName=EmailTextBox}" Style="{StaticResource ErrorTextStyle}" />


                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Пароль" Style="{StaticResource FormLabelStyle}"/>
                        <TextBlock Text="*" Foreground="{StaticResource BrushTerracotta}" Style="{StaticResource FormLabelStyle}" Margin="2,5,0,5"/>
                    </StackPanel>
                    <TextBox x:Name="PasswordTextBox" Text="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Style="{StaticResource FormTextBoxStyle}"/>
                    <TextBlock Text="{Binding (Validation.Errors)[0].ErrorContent, ElementName=PasswordTextBox}" Style="{StaticResource ErrorTextStyle}" />

                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Подтверждение пароля" Style="{StaticResource FormLabelStyle}"/>
                        <TextBlock Text="*" Foreground="{StaticResource BrushTerracotta}" Style="{StaticResource FormLabelStyle}" Margin="2,5,0,5"/>
                    </StackPanel>
                    <TextBox x:Name="ConfirmPasswordTextBox" Text="{Binding ConfirmPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Style="{StaticResource FormTextBoxStyle}"/>
                    <TextBlock Text="{Binding (Validation.Errors)[0].ErrorContent, ElementName=ConfirmPasswordTextBox}" Style="{StaticResource ErrorTextStyle}" />

                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Роль" Style="{StaticResource FormLabelStyle}"/>
                        <TextBlock Text="*" Foreground="{StaticResource BrushTerracotta}" Style="{StaticResource FormLabelStyle}" Margin="2,5,0,5"/>
                    </StackPanel>
                    <ComboBox x:Name="RoleComboBox" ItemsSource="{Binding AllRoles}" 
                              SelectedItem="{Binding SelectedRole, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                              DisplayMemberPath="Name"
                              Style="{StaticResource FormComboBoxStyle}"/>
                    <TextBlock Text="{Binding (Validation.Errors)[0].ErrorContent, ElementName=RoleComboBox}" Style="{StaticResource ErrorTextStyle}" />
                </StackPanel>
            </ScrollViewer>

            <StackPanel Grid.Row="2" Margin="0,20,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Отмена" Command="{Binding CancelCommand}" Style="{StaticResource SecondaryButtonStyle}"/>
                    <Button Content="Добавить" Command="{Binding AddUserCommand}" Style="{StaticResource PrimaryActionButtonStyle}" Margin="10,0,0,0"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>

<UserControl x:Class="Plantify.Views.MyGardenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Plantify.Views"
             xmlns:vm="clr-namespace:Plantify.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:MyGardenViewModel, IsDesignTimeCreatable=False}"
             Background="{StaticResource BrushMilkWhite}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Tasks Summary Bar -->
        <Border Grid.Row="0" 
                Background="{StaticResource BrushSand}" 
                BorderBrush="{StaticResource BrushTerracotta}"
                BorderThickness="0,0,0,1"
                Padding="20,10" 
                Margin="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           HorizontalAlignment="Left" VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource H2}">
                            <Setter Property="Text" Value="{Binding TasksSummary}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsInMassSelectionMode}" Value="True">
                                    <Setter Property="Text" Value="{Binding TasksSummary}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                
                <Button Grid.Column="1" 
                        HorizontalAlignment="Right" VerticalAlignment="Center"
                        Command="{Binding MarkAllTasksAsCompletedCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
                            <Setter Property="Content" Value="Отметить все"/>
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsInMassSelectionMode}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Grid.Column="1" 
                        HorizontalAlignment="Right" VerticalAlignment="Center"
                        Command="{Binding CancelMassSelectionCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
                            <Setter Property="Content" Value="Отменить"/>
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsInMassSelectionMode}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>

        <!-- Plant Grid -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="30,0,0,0">
            <ScrollViewer.Style>
                <Style TargetType="ScrollViewer" BasedOn="{StaticResource ScrollBarStyle}">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ShowEmptyState}" Value="True">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ScrollViewer.Style>
            <ItemsControl ItemsSource="{Binding UserPlants}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" CornerRadius="12" Width="200" Margin="0,0,20,20">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="4" BlurRadius="15" Color="{StaticResource ColorForestGreen}" Opacity="0.1"/>
                            </Border.Effect>
                            <StackPanel>
                                <!-- Image -->
                                <Grid Width="170" Height="130" Margin="15,15,15,0">
                                    <Border CornerRadius="8" ClipToBounds="True">
                                        <Border.Background>
                                            <ImageBrush ImageSource="{Binding DisplayImageSource}" Stretch="UniformToFill"/>
                                        </Border.Background>
                                    </Border>
                                    
                                    <!-- Location Pill -->
                                    <Border HorizontalAlignment="Left" VerticalAlignment="Top" Margin="8" Padding="8,4" CornerRadius="12"
                                            Background="{StaticResource BrushPureWhite80Transparent}">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Location}" Value="{x:Null}">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Location}" Value="">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding Location}" Foreground="{StaticResource BrushDarkGreen}" FontSize="12" FontWeight="SemiBold"/>
                                    </Border>

                                    <!-- Task Completion CheckBox -->
                                    <CheckBox HorizontalAlignment="Right" VerticalAlignment="Top" Margin="8" 
                                              Style="{StaticResource TaskCompletionCheckBoxStyle}"
                                              IsChecked="{Binding IsTaskCompletedToday, Mode=TwoWay}"/>
                                </Grid>
                                
                                <!-- Info Section -->
                                <StackPanel Margin="15,10,15,15">
                                    <TextBlock Text="{Binding Name}" Style="{StaticResource H2}" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding Species}" Foreground="Gray" FontSize="12" Margin="0,2,0,10"/>

                                    <!-- Characteristics (Example) -->
                                    <StackPanel Orientation="Vertical" Margin="0,0,0,15">
                                        <Border Background="{StaticResource BrushLightGray}" CornerRadius="12" Padding="8,4" Margin="0,0,0,5" HorizontalAlignment="Left">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="☀️" VerticalAlignment="Center"/>
                                                <TextBlock Text="{Binding LightRequirement}" FontSize="12" Margin="4,0,0,0" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                        <Border Background="{StaticResource BrushLightGray}" CornerRadius="12" Padding="8,4" HorizontalAlignment="Left">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="🌡️" VerticalAlignment="Center"/>
                                                <TextBlock Text="20-25°C" FontSize="12" Margin="4,0,0,0" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>

                                    <!-- Watering and Fertilizing -->
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="💧 Полив" Foreground="Gray" FontSize="12"/>
                                            <TextBlock Text="{Binding NextWateringDue}" FontWeight="Bold" Margin="0,2,0,0"
                                                       Foreground="{Binding DaysToNextWatering, Converter={StaticResource NextCareActionToBrushConverter}}" FontSize="14"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="🌱 Удобрение" Foreground="Gray" FontSize="12"/>
                                            <TextBlock Text="{Binding NextFertilizingDue}" FontWeight="Bold" Margin="0,2,0,0"
                                                       Foreground="{Binding DaysToNextFertilizing, Converter={StaticResource NextCareActionToBrushConverter}}" FontSize="14"/>
                                        </StackPanel>
                                    </Grid>

                                    <!-- Action Buttons -->
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                                        <Button Style="{StaticResource SecondaryButtonStyle}" Padding="15,5" Width="60" Height="30" Margin="0,0,5,0"
                                                Command="{Binding DataContext.EditPlantCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}">
                                            <TextBlock Text="✏️" FontSize="14"/>
                                        </Button>
                                        <Button Style="{StaticResource DeletePlantButtonStyle}" Padding="15,5" Width="60" Height="30"
                                                Command="{Binding DataContext.DeletePlantCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}">
                                            <TextBlock Text="🗑️" FontSize="14"/>
                                        </Button>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        <!-- Empty State -->
        <Grid Grid.Row="1">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ShowEmptyState}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,50">
                <Image Source="/Images/MyGardenPlaceholder.png" Width="200" Height="200" Margin="20"/>
                <TextBlock Text="У вас пока нет растений в саду"
                           FontSize="20"
                           Foreground="Gray"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,20"/>
                <Button Content="+ Добавить растение" 
                        Command="{Binding AddPlantCommand}"
                        Background="{StaticResource BrushForestGreen}" 
                        Foreground="{StaticResource BrushPureWhite}"
                        Padding="15,8"
                        BorderThickness="0">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource BrushLightForestGreen}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Confirm Care Buttons -->
        <StackPanel Grid.Row="1" 
                    Orientation="Horizontal"
                    HorizontalAlignment="Right" 
                    VerticalAlignment="Bottom"
                    Margin="0,0,20,20">
            <StackPanel.Style>
                <Style TargetType="StackPanel">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsAnyPlantSelected}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>

            <Button Content="Полить" 
                    Command="{Binding ConfirmWaterActionCommand}"
                    Style="{StaticResource PrimaryActionButtonStyle}"/>

            <Button Content="Удобрить" 
                    Command="{Binding ConfirmFertilizeActionCommand}"
                    Style="{StaticResource FertilizeActionButtonStyle}"
                    Margin="10,0,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
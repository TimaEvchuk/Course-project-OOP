<UserControl x:Class="Plantify.Views.EncyclopediaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Plantify.Views"
             xmlns:vm="clr-namespace:Plantify.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:EncyclopediaViewModel, IsDesignTimeCreatable=False}"
             Background="{StaticResource BrushMilkWhite}">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Справочник растений" Style="{StaticResource H1}" Margin="0,0,0,20"/>

        <!-- Search Box -->
        <Border Grid.Row="1" BorderBrush="{StaticResource BrushBorderGray}" BorderThickness="1" CornerRadius="5" Padding="5" Width="400" HorizontalAlignment="Left" Background="{StaticResource BrushPureWhite}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="🔍" VerticalAlignment="Center" Margin="5,0"/>
                <TextBlock Grid.Column="1" Text="Найдите растение, болезнь или симптом..." IsHitTestVisible="False" VerticalAlignment="Center" Foreground="Gray" Margin="5,0,0,0">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SearchText}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                <TextBox Grid.Column="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" BorderThickness="0" Background="Transparent"
                         Style="{StaticResource SearchTextBoxStyle}"/>
            </Grid>
        </Border>
        
        <!-- Filter Pills -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,20,0,10">
            <RadioButton Content="Все" IsChecked="True" Command="{Binding SelectFilterCommand}" CommandParameter="Все" Style="{StaticResource FilterPillRadioButtonStyle}"/>
            <RadioButton Content="Для новичков" Command="{Binding SelectFilterCommand}" CommandParameter="Для новичков" Style="{StaticResource FilterPillRadioButtonStyle}"/>
            <RadioButton Content="Тенелюбивые" Command="{Binding SelectFilterCommand}" CommandParameter="Тенелюбивые" Style="{StaticResource FilterPillRadioButtonStyle}"/>
            <RadioButton Content="Болезни и вредители" Command="{Binding SelectFilterCommand}" CommandParameter="Болезни и вредители" Style="{StaticResource FilterPillRadioButtonStyle}"/>
        </StackPanel>
        
        <!-- Plant List -->
        <ListBox Grid.Row="3" ItemsSource="{Binding Plants}" HorizontalContentAlignment="Stretch" ScrollViewer.HorizontalScrollBarVisibility="Disabled" BorderThickness="0" Background="{StaticResource BrushMilkWhite}"
                 ScrollViewer.CanContentScroll="False">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border CornerRadius="12" Background="{StaticResource BrushPureWhite}" Margin="0,0,24,24" MinHeight="150">
                        <Border.Effect>
                            <DropShadowEffect ShadowDepth="8" BlurRadius="24" Color="{StaticResource ColorForestGreen}" Opacity="0.1"/>
                        </Border.Effect>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <Border CornerRadius="12" Margin="10">
                                <Border.Background>
                                    <ImageBrush ImageSource="{Binding DisplayImageSource}" Stretch="UniformToFill"/>
                                </Border.Background>
                            </Border>
                            
                            <Grid Grid.Column="1" Margin="10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <TextBlock Grid.Row="0" Text="{Binding Name}" Style="{StaticResource H2}" TextWrapping="Wrap" Margin="0,0,80,0"/>
                                <TextBlock Grid.Row="1" Text="{Binding Description}" Style="{StaticResource Body}" TextWrapping="Wrap" Margin="0,5,0,0" VerticalAlignment="Top"/>
                                <TextBlock Grid.Row="2" Text="Читать →" Style="{StaticResource Caption}" Foreground="{StaticResource BrushForestGreen}" VerticalAlignment="Bottom" HorizontalAlignment="Right"/>
                            </Grid>
                            
                            <Border Grid.ColumnSpan="2" Background="{StaticResource BrushSand}" CornerRadius="10" Padding="8,4" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="15">
                                <TextBlock Text="{Binding Difficulty}" Foreground="{StaticResource BrushDarkGreen}" FontSize="12"/>
                            </Border>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>

<UserControl x:Class="Plantify.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Plantify.Views"
             xmlns:vm="clr-namespace:Plantify.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:DashboardViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="{Binding WelcomeMessage}" FontSize="32" FontWeight="Bold" HorizontalAlignment="Center"/>
            <Button Content="View Plant Encyclopedia" Command="{Binding GoToEncyclopediaCommand}" Margin="0,20,0,0" Width="220" Height="40" FontSize="16"/>
            <Button Content="Manage Plants" Command="{Binding GoToPlantManagementCommand}" Margin="0,10,0,0" Width="220" Height="40" FontSize="16"/>
        </StackPanel>
    </Grid>
</UserControl>

<UserControl x:Class="Plantify.Views.AddUserPlantView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Plantify.Views"
             xmlns:vm="clr-namespace:Plantify.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:AddUserPlantViewModel, IsDesignTimeCreatable=False}">
    <Border Background="{StaticResource BrushPureWhite}" CornerRadius="12" Padding="30" MinWidth="400">
        <StackPanel>
            <TextBlock Text="Добавить растение в сад" Style="{StaticResource H1}" Margin="0,0,0,20"/>

            <TextBlock Text="Выберите растение" Margin="0,10,0,5}"/>
            <ComboBox ItemsSource="{Binding AllPlants}"
                      SelectedItem="{Binding SelectedPlant}"
                      DisplayMemberPath="Name"/>

            <TextBlock Text="Имя (необязательно)" Margin="0,10,0,5}"/>
            <TextBox Text="{Binding CustomName, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Местоположение" Margin="0,10,0,5}"/>
            <TextBox Text="{Binding Location, UpdateSourceTrigger=PropertyChanged}"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,30,0,0">
                <Button Content="Отмена" Command="{Binding CancelCommand}" Style="{StaticResource SecondaryButtonStyle}"/>
                <Button Content="Сохранить" Command="{Binding SaveCommand}" Margin="10,0,0,0"
                        Background="{StaticResource BrushForestGreen}"
                        Foreground="{StaticResource BrushPureWhite}"
                        Padding="20,10"
                        BorderThickness="0">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource BrushLightForestGreen}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>

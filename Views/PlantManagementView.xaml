<UserControl x:Class="Plantify.Views.PlantManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Plantify.Views"
             xmlns:vm="clr-namespace:Plantify.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:PlantManagementViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Plant List -->
        <DataGrid Grid.Column="0" ItemsSource="{Binding Plants}" SelectedItem="{Binding SelectedPlant}"
                  AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Difficulty" Binding="{Binding Difficulty}" Width="100"/>
                <DataGridTextColumn Header="Light" Binding="{Binding LightRequirement}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Spacer -->
        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Center" VerticalAlignment="Stretch"/>

        <!-- Edit/Add Form -->
        <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="10,0,0,0">
                <TextBlock Text="Manage Plant" FontSize="20" FontWeight="Bold"/>

                <TextBlock Text="Name" Margin="0,15,0,0"/>
                <TextBox Text="{Binding PlantName, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="Description" Margin="0,10,0,0"/>
                <TextBox Text="{Binding PlantDescription, UpdateSourceTrigger=PropertyChanged}" TextWrapping="Wrap" AcceptsReturn="True" Height="80"/>

                <TextBlock Text="Watering Interval (days)" Margin="0,10,0,0"/>
                <TextBox Text="{Binding PlantWateringInterval, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="Light Requirement" Margin="0,10,0,0"/>
                <TextBox Text="{Binding PlantLightRequirement, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="Difficulty" Margin="0,10,0,0"/>
                <TextBox Text="{Binding PlantDifficulty, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="Image" Margin="0,10,0,0"/>
                <Image Source="{Binding PlantImagePath}" Height="100" Margin="0,5,0,0" Stretch="Uniform" HorizontalAlignment="Left"/>
                <Button Content="Select Image" Command="{Binding SelectImageCommand}" Width="120" HorizontalAlignment="Left" Margin="0,5,0,0"/>

                <StackPanel Orientation="Horizontal" Margin="0,20,0,0">                    <Button Content="Add New" Command="{Binding AddPlantCommand}" Width="100"/>
                    <Button Content="Update Selected" Command="{Binding UpdatePlantCommand}" Width="120" Margin="10,0,0,0"/>
                    <Button Content="Delete Selected" Command="{Binding DeletePlantCommand}" Width="120" Margin="10,0,0,0" Background="IndianRed"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>

<UserControl x:Class="Plantify.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Plantify.Views"
             xmlns:converters="clr-namespace:Plantify.Converters"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Левая колонна -->
        <StackPanel Grid.Column="0" Orientation="Vertical">
            <!-- Раздел "Аккаунт" -->
            <Border Style="{StaticResource StatWidgetStyle}" Padding="20">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Управление аккаунтом" Style="{StaticResource H2}" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

                    <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                        <Button Content="Сменить пароль" Command="{Binding ShowChangePasswordOverlayCommand}" Style="{StaticResource PrimaryActionButtonStyle}"/>
                        <Button Content="Выйти из аккаунта" Command="{Binding LogoutCommand}" Style="{StaticResource DestructiveButtonStyle}" Margin="10,0,0,0"/>
                    </StackPanel>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Avatar -->
                        <StackPanel Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Top">
                            <Grid Width="150" Height="150">
                                <Ellipse Fill="{StaticResource BrushLightForestGreen}"/>
                                <Ellipse>
                                    <Ellipse.Fill>
                                        <ImageBrush ImageSource="{Binding UserAvatarPath}"
                                                    Stretch="UniformToFill"/>
                                    </Ellipse.Fill>
                                </Ellipse>
                            </Grid>
                        </StackPanel>

                        <!-- User Info -->
                        <StackPanel Grid.Column="1" Margin="30,0,0,0" VerticalAlignment="Top">
                            <TextBlock Text="{Binding UserLogin}" Style="{StaticResource H3}" FontSize="22" FontWeight="Bold"/>
                            <TextBlock Text="{Binding UserRole}" FontStyle="Italic" Foreground="{StaticResource BrushDarkGray}" FontSize="16" Margin="0,5,0,5"/>
                            <TextBlock Text="{Binding UserEmail}" Foreground="{StaticResource BrushDarkGray}" FontSize="16" Margin="0,0,0,10"/>
                            <Button Content="Изменить аватар" Command="{Binding ChangeAvatarCommand}"
                                    Style="{StaticResource SecondaryButtonStyle}" HorizontalAlignment="Left"/>
                        </StackPanel>
                    </Grid>
                </DockPanel>
            </Border>

            <!-- Раздел "Уведомления" -->
            <Border Style="{StaticResource StatWidgetStyle}" Padding="20" Margin="0,20,0,0">
                <StackPanel>
                    <TextBlock Text="Управление уведомлениями" Style="{StaticResource H2}" FontSize="24" FontWeight="Bold" Margin="0,0,0,15"/>
                    <CheckBox IsChecked="{Binding EnableCareNotifications}" Content="Напоминания об уходе" Margin="0,0,0,10"/>
                    <CheckBox IsChecked="{Binding EnableSuccessNotifications}" Content="Подтверждения действий"/>
                </StackPanel>
            </Border>

        </StackPanel>

        <!-- Правая колонна -->
        <Grid Grid.Column="2">
            <!-- Раздел "Подписка" -->
            <Border Style="{StaticResource StatWidgetStyle}" Padding="20">
                <StackPanel>
                    <TextBlock Text="Управление тарифом" Style="{StaticResource H2}" FontSize="24"
                               FontWeight="Bold" Margin="0,0,0,15"/>

                    <TextBlock Text="{Binding SubscriptionStatusText}" VerticalAlignment="Center"
                               Margin="0,0,0,15">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsPremium}" Value="False">
                                        <Setter Property="FontSize" Value="20"/>
                                        <Setter Property="Foreground" Value="{StaticResource BrushDarkGray}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsPremium}" Value="True">
                                        <Setter Property="FontSize" Value="20"/>
                                        <Setter Property="Foreground" Value="Goldenrod"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <StackPanel Margin="0,0,0,15" Visibility="{Binding IsPremium, Converter={StaticResource
                                                      BooleanToVisibilityConverter}}">
                        <TextBlock FontSize="16">
                            <Run Text="Дата начала:"/>
                            <Run Text="{Binding PremiumStartDate, StringFormat=dd.MM.yyyy}" FontWeight="Bold"/>
                        </TextBlock>
                        <TextBlock Margin="0,5,0,0" FontSize="16">
                            <Run Text="Дата окончания:"/>
                            <Run Text="{Binding PremiumEndDate, StringFormat=dd.MM.yyyy}" FontWeight="Bold"/>
                        </TextBlock>
                    </StackPanel>

                    <Button Content="{Binding SubscriptionButtonText}" Command="{Binding
                                       SubscriptionActionCommand}" HorizontalAlignment="Left" Margin="0,0,0,15">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource PrimaryActionButtonStyle}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsPremium}" Value="True">
                                        <Setter Property="Background" Value="{StaticResource BrushTerracotta}"/>
                                        <Setter Property="Foreground" Value="{StaticResource BrushPureWhite}"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border x:Name="ButtonBorder" Background="{TemplateBinding
                                                                Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center"
                                                                          VerticalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="ButtonBorder"
                                                                    Property="Background" Value="{StaticResource BrushDarkTerracotta}"/>
                                                        </Trigger>
                                                        <Trigger Property="IsEnabled" Value="False">
                                                            <Setter TargetName="ButtonBorder"
                                                                    Property="Background" Value="{StaticResource BrushLightGray}"/>
                                                            <Setter Property="Foreground" Value="{StaticResource
                                                                BrushGray}"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <TextBlock TextWrapping="Wrap" Foreground="{StaticResource BrushDarkGray}" MaxWidth="400" TextAlignment="Left"
                               Visibility="{Binding IsPremium, Converter={StaticResource
                               InverseBooleanToVisibilityConverter}}">
                        <Run Text="*При приобретении Премиум тарифа, вы получаете возможность добавлять неограниченное количество растений в свой сад, а также смотреть сводку задач на месяц вперед."/>
                    </TextBlock>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>

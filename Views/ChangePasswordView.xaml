<UserControl x:Class="Plantify.Views.ChangePasswordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:Plantify.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:ChangePasswordViewModel}"
             d:DesignHeight="350" d:DesignWidth="400">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Styles/Typography.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/Styles/TextBoxes.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/Styles/Buttons.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Border Background="{StaticResource BrushMilkWhite}" CornerRadius="12" Padding="30" Width="400">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="Сменить пароль" Style="{StaticResource H2}" TextWrapping="Wrap"/>
                <Button Command="{Binding CancelCommand}" Style="{StaticResource CloseButtonStyle}" 
                        HorizontalAlignment="Right" VerticalAlignment="Top" Width="35" Height="35" Margin="0,-25,-25,0"/>
            </Grid>

            <TextBlock Grid.Row="1" Style="{StaticResource FormLabelStyle}">
                <Run Text="Текущий пароль"/>
                <Run Text="*" Foreground="{StaticResource BrushTerracotta}"/>
            </TextBlock>
            <TextBox Grid.Row="2" x:Name="CurrentPasswordBox" Style="{StaticResource FormTextBoxStyle}" FontSize="14" Text="{Binding CurrentPassword, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Grid.Row="3" Style="{StaticResource FormLabelStyle}">
                <Run Text="Новый пароль"/>
                <Run Text="*" Foreground="{StaticResource BrushTerracotta}"/>
            </TextBlock>
            <PasswordBox Grid.Row="4" x:Name="NewPasswordBox" Style="{StaticResource FormPasswordBoxStyle}" FontSize="14" PasswordChanged="PasswordBox_PasswordChanged" />

            <TextBlock Grid.Row="5" Style="{StaticResource FormLabelStyle}">
                <Run Text="Подтвердите новый пароль"/>
                <Run Text="*" Foreground="{StaticResource BrushTerracotta}"/>
            </TextBlock>
            <PasswordBox Grid.Row="6" x:Name="ConfirmNewPasswordBox" Style="{StaticResource FormPasswordBoxStyle}" FontSize="14" PasswordChanged="PasswordBox_PasswordChanged" />

            <TextBlock Grid.Row="7" Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,10,0,0"/>

            <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,30,0,0">
                <Button Content="Отмена" Command="{Binding CancelCommand}" Style="{StaticResource SecondaryButtonStyle}"/>
                <Button Content="Сменить" Command="{Binding ChangePasswordCommand}" Style="{StaticResource PrimaryActionButtonStyle}" Margin="10,0,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>